# Design System

> Run `/design system` to populate this file through a guided conversation.
> Once written, this is consumed by /dev (build with it), /qa (check against it), /review (flag violations).

<!-- Placeholder — run /design system to generate -->
